# Claude skills

Claude Code skills live under `~/.claude/skills/<skill-name>/` with a required
`SKILL.md`. This folder holds templates you can copy into that structure.

Add short, task-scoped skills here. Prefer linking to `docs/` for detailed
standards instead of embedding long guidance in skills.
